package myPackage.Pages;
import java.util.*;

public class Main {
    public static void main(String[] args) throws InterruptedException {

        // System.out.println(AnagramString.function("abc", "cby"));
        System.out.println(AddStrings.addStrings("11", "12"));

        // String result = LongestStringInArray.longest("ABC 1123C 1123C B");
        // System.out.println(result);

        // String original = "Pratik Bhowmik";
        
        // Map<Character,Integer> mp = new HashMap<>();

        // for (int i = 0; i < original.length(); i++) {
        //     if (mp.containsKey(original.charAt(i))) {
        //         mp.put(original.charAt(i), mp.get(original.charAt(i))+1);
        //     } else {
        //         mp.put(original.charAt(i), 1);
        //     }
        // }

        // for (Map.Entry<Character,Integer> mpel : mp.entrySet()) {
        //     if (mpel.getValue() > 1) {
        //         System.out.println(mpel.getKey()+ ", count = "+mpel.getValue());
        //     }
            
        // }
        // System.out.println(AppmetryCompanyCode.canFormPalindrome("MADAM"));
        // Appmetry.canFormPalindrome("Pratik Bhowmik");

    }
}
